# OpenCode Adapter

This adapter contains optional OpenCode scaffold files for MALTS.

## Install

Dry-run:

```powershell
.\scripts\Install-MALTS.ps1 -Tool OpenCode
```

Apply:

```powershell
.\scripts\Install-MALTS.ps1 -Tool OpenCode -Apply
```

## Contents

- `AGENTS.example.md` starting template installed as `AGENTS.md`, including response-quality guardrails
- `opencode.json`
- `.opencode/agents/`
- `MALTS_BOOT.md` generated by the installer to point at the shared `MALTS_ROOT`

The default `ManagedMerge` mode updates only the marked MALTS instruction block in `AGENTS.md` and preserves surrounding user rules. Use `InstructionMode Skip` to leave it untouched; full-file `Replace` requires explicit `Overwrite`.
- `skills/<name>/SKILL.md` lightweight discovery bridges that route to shared implementations

Install copies OpenCode scaffold files, installs one shared `MALTS_ROOT`, generates `MALTS_BOOT.md`, and installs discovery bridges. It does not create a full tool-local runtime or skill implementation copy.

Review existing user files before copying. Do not overwrite without explicit confirmation.

## Runtime Rule

English runtime docs are default. Localized CH runtime references are used only for explicit Chinese review, editing, or documentation synchronization.

Canonical MALTS artifacts:

```text
PROJECT_CONTROL.md
WORK_TASK_REPORT.md
PROJECT_HANDOFF.md
```

Translated mirrors such as `项目控制.md`, `工作任务报告.md`, and `项目交接.md` are optional; create them only when explicitly requested or when an external workflow requires a full translated mirror. Chinese-facing Markdown should be valid UTF-8. On Windows, prefer UTF-8 with BOM unless the local project convention conflicts.

## Multi-Agent Gate

OpenCode agents may be used only after MALTS launch review and the user's explicit `确认运行`. Record model policy, dispatch evidence, and report recycling in `PROJECT_CONTROL.md`.

## Handoff

Default Agent-facing handoff:

```text
PROJECT_HANDOFF.md
```
