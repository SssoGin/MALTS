# MALTS Codex project-scoped configuration scaffold.
# Copy only after reviewing with existing Codex config. Project-scoped config
# loads only when the project is trusted by Codex.
# Model and reasoning-effort overrides are intentionally absent from the role
# scaffolds. Select them per task from the launch review and current runtime
# evidence; responsibility names do not imply difficulty.

[agents]
max_threads = 6
max_depth = 1
